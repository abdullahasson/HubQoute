
// mui
import { Box , Typography } from "@mui/material"

const Heading = ({
    title
}) => {

    return (
        <Box>
            <Typography>
                {title}
            </Typography>
        </Box>
    )

}

export default Heading